<div class="col" id="notice"><%= notice %></div>

<div class="col">
  <h1>Your cart</h1>
</div>

<div class="col">
  <table class="cart_detail">
    <tr>
      <th>Book</th>
      <th>Unit price</th>
      <th>Quantity</th>
    </tr>
    <% @cart.line_items.each do |item| %>
    <tr>
      <td><%= item.product.title %></td>
      <td><%= number_to_currency(item.product.price) %></td>
      <td><%= item.quantity %></td>
    </tr>
    <% end %>
  </table>
  <p>Total: <b><%= number_to_currency(@cart.total_price) %></b></p>
  <%= button_to 'Check Out', '#', class: 'check_out' %>
  <%= button_to 'Empty cart', @cart, method: :delete, data: { confirm: 'Are you sure?' }, class: 'empty_cart' %><br>
  <%= link_to 'Continue shopping >>>', root_path %>
</div>
